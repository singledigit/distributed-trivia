{
  "name": "session-orchestrator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=es2022 --outdir=dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.800.0",
    "@aws-sdk/lib-dynamodb": "^3.800.0",
    "@aws-sdk/client-lambda": "^3.800.0",
    "@aws/durable-execution-sdk-js": "^1.1.1"
  },
  "devDependencies": {
    "@trivia/shared": "file:../../layers/shared/nodejs/node_modules/@trivia/shared",
    "@types/aws-lambda": "^8.10.149",
    "typescript": "^5.8.3",
    "esbuild": "^0.25.0"
  }
}
